<% for (var i = 0, l = sections.size(); i<l; i++) { %><% var section = sections[i]; %><%= section.docsText %><% if (!(section.codeText ==~ /^\s*$/)) { %>
```<%= section.codeText %>```
<% } %>
<% } %>
